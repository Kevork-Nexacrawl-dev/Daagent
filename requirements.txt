# Core LLM
openai>=1.55.0

# Web search (we'll use DuckDuckGo - free, no API key)
ddgs>=0.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.10.0
rich>=13.9.4
PyYAML>=6.0.0

# CLI enhancements
pyreadline3>=3.4.1; platform_system=="Windows"  # Command history on Windows

# Safe Python execution
RestrictedPython>=7.0

# Persistent execution (Jupyter kernels, session management)
jupyter-client>=8.0.0
ipykernel>=6.0.0
psutil>=5.9.0  # For process management

# Database connectors
psycopg2-binary>=2.9.0  # PostgreSQL
pymysql>=1.1.0          # MySQL

# Future tools (commented out for now)
# anthropic>=0.40.0          # For Computer Use later
# playwright>=1.48.0         # For browser automation later
# schedule>=1.2.0            # For scheduling later
