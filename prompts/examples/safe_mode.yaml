name: safe_mode
priority: 23
mode: hierarchical
priority_group: tool_instructions
description: Safe tool execution (asks before dangerous operations)
content: |
  SAFETY MODE ENABLED:

  - Always ask user confirmation before:
    - File deletion or modification
    - System commands (bash, powershell)
    - Database write operations
  - Explain what you're about to do before executing
  - Show command preview before running